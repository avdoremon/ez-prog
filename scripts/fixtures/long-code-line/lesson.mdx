---
title: Long Code Line Lesson
description: Fixture for the code-line-length rule.
order: 0
---

Short prose before a fence whose longest line overflows the rendered column.

```js
const ok = 'this line is comfortably inside the limit';
if (!settled.has(i) && dist[i] < (node < 0 ? Infinity : dist[node])) node = i;
```
