---
title: Binary Search
description: Clean fixture exercising the real binary-search viz end to end.
order: 0
viz: binary-search
---

A short, valid lesson used to prove the linter reports no errors when every rule is satisfied.

The fenced line below is exactly CODE_LINE_LIMIT characters long, pinning the
boundary as inclusive: at the limit is allowed, one over is not.

```js
const atTheLimit = 'exactly sixty-eight characters wide, inclusive';
```
