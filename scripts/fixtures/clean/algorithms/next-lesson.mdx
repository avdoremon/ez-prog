---
title: Next Lesson
description: Clean fixture whose prerequisite must resolve correctly on Windows paths.
viz: binary-search
order: 1
# prerequisites is deliberately the LAST frontmatter field here too, to
# prove the fix does not false-positive on a valid last-field prerequisite.
prerequisites:
  - /algorithms/binary-search
---

This lesson depends on the previous one; the prerequisite slug must resolve even when readdir returns backslash paths.
