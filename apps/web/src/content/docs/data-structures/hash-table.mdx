---
title: Hash Tables
description: An array can find slot 5 instantly but not the value 37 — unless you compute the slot from the value itself.
order: 214
difficulty: intermediate
estimatedMinutes: 8
viz: hash-table
prerequisites: [/data-structures/array]
languages: [js, py, c]
---

import Viz from '../../../components/Viz.astro';
import RunnableCode from '../../../components/RunnableCode.tsx';

An array reaches slot 5 instantly, but finding the *value* 37 means looking
through it. A hash table closes that gap with one idea: if the value decides
which slot it goes in, you can compute where to look instead of searching.

<Viz id="hash-table" />

## How it works

Pick a slot from the key by arithmetic — `key % capacity`, seven slots, so
12 goes to slot 5; finding it again means the same sum. No scan, at any
table size.

19 also hashes to slot 5 — a **collision**, unavoidable since keys outnumber
slots. **Linear probing** resolves it: try the next slot, wrapping at the
end. Watch 19 walk 5 → 6 → 0 to the first free slot — so **19 is no longer
at its own hash**, and a lookup must repeat that walk: three looks, not
seven.

The walk stops at the first *empty* slot, which is why a failed lookup is
cheap: an empty slot proves the key was never inserted.

## Try it

<RunnableCode lang="js" client:visible source={`class HashTable {
  constructor(capacity) {
    this.capacity = capacity;
    this.slots = new Array(capacity).fill(null);
  }

  insert(key) {
    let i = key % this.capacity;
    for (let n = 0; n < this.capacity; n++) {
      if (this.slots[i] === null) {
        this.slots[i] = key;
        return true;
      }
      if (this.slots[i] === key) return true;
      i = (i + 1) % this.capacity;
    }
    return false;
  }

  has(key) {
    let i = key % this.capacity;
    for (let n = 0; n < this.capacity; n++) {
      if (this.slots[i] === key) return true;
      if (this.slots[i] === null) return false;
      i = (i + 1) % this.capacity;
    }
    return false;
  }
}

const table = new HashTable(7);
for (const key of [12, 19, 26]) table.insert(key);
console.log('slots', table.slots);
console.log('has 19', table.has(19));
console.log('has 20', table.has(20));
`} />

## Trade-offs

The `load` factor — keys over slots — decides speed: short probes near
empty, long near full. Real tables resize past load 0.7, rehashing
everything — the [amortized cost](/complexity/amortized-analysis/) argument.

A hash table gives up order — slots come out hash-ordered, so a
[BST](/data-structures/bst/) still wins for ranges — and easy deletion,
since blanking a slot breaks every probe chain through it; real
implementations use a *tombstone*.

## Check your understanding

1. **Why can't a lookup for 19 just check slot 5 and stop?**
   - It could — the hash always gives the right slot
   - Because a collision displaced 19 further along, so the lookup must
     repeat the insert's walk
   - Because slot 5 is empty
   - Because the table is sorted by hash

   <details>
   <summary>Answer</summary>

   The collision at slot 5 pushed 19 to the first free slot, so its hash is
   where the search *starts*, not where the key is. Slot 5 is occupied — by
   12 — which is exactly the problem. Nothing here is sorted at all.
   </details>

2. **You delete a key by setting its slot back to empty. What breaks?**
   - Nothing — that is how deletion works
   - Keys placed after it by probing past that slot become unreachable,
     because a lookup now stops at the gap
   - The table's capacity shrinks
   - Only the deleted key is affected

   <details>
   <summary>Answer</summary>

   An empty slot tells a lookup to give up, so a gap punched into a probe
   chain hides everything beyond it — those keys are still there but can no
   longer be found. Capacity is fixed and unaffected. Hence tombstones.
   </details>

3. **Your table has 7 slots and holds 6 keys. What has happened to lookups?**
   - Nothing — hash tables are constant-time by definition
   - They have got slower: long runs of occupied slots mean long probes
   - They have got faster, since more keys are present
   - They fail

   <details>
   <summary>Answer</summary>

   A load factor of 0.86 leaves few gaps, so probe runs are long and a miss
   may walk nearly the whole table. Constant-time is the *average* while load
   stays low, not a guarantee — which is why real tables resize rather than
   fill up. Lookups still succeed, just slowly.
   </details>

## Practice

Try [Design HashMap on LeetCode](https://leetcode.com/problems/design-hashmap/)
— building one without the language's own map is how you feel the collision
handling.
