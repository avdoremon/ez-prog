---
title: Amortized Analysis
description: Some operations are occasionally expensive but cheap on average — and averaging them honestly needs its own kind of accounting.
order: 236
difficulty: intermediate
estimatedMinutes: 7
viz: amortized-growth
prerequisites: [/complexity/big-o]
languages: [js, py, c]
---

import Viz from '../../../components/Viz.astro';
import RunnableCode from '../../../components/RunnableCode.tsx';

Appending to a list feels free. But an array is a fixed block, so eventually
an append finds no room and must move everything somewhere bigger. Amortized
analysis is how we say "that is still cheap" without lying.

<Viz id="amortized-growth" />

## How it works

A dynamic array tracks how many values it holds and how many it *could* hold.
Appending writes into a spare slot — one step. When the spares run out, it
allocates a bigger block, copies everything across, then appends.

Copying *n* values costs *n*, so a single append's worst case is O(*n*), and
averaging doesn't change that.

What saves it is how rarely that happens: the array doesn't grow by one slot,
it **doubles**, so each resize buys twice the room of the last, and resizes
get rarer exactly as fast as they get costlier.

Count it above: from capacity 1, sixteen appends trigger four resizes copying
1, 2, 4, and 8 values — fifteen copies total, summing to just under **twice**
the final size, so copying averages under two per append however long the
run (not one, however tempting that sounds). This is what "amortized O(1)"
means: not every append is cheap, but *n* appends together cost O(*n*).

## Try it

<RunnableCode lang="js" client:visible source={`function push(arr, value) {
  if (arr.size === arr.capacity) {
    const bigger = new Array(arr.capacity * 2);
    for (let i = 0; i < arr.size; i++) bigger[i] = arr.data[i];
    arr.data = bigger;
    arr.capacity *= 2;
  }
  arr.data[arr.size++] = value;
  return arr;
}

const arr = { data: new Array(2), size: 0, capacity: 2 };
for (const value of [1, 2, 3, 4, 5]) push(arr, value);
arr.data.slice(0, arr.size);
`} />

## Trade-offs

Doubling makes the argument work; a *fixed* amount destroys it — adding ten
slots at a time keeps resizes equally expensive without ever getting rarer,
costing about *n*²/20 over *n* appends.

The cost is wasted space: right after a resize the array may be half empty,
trading up to 2× memory for constant-time appends (some implementations grow
by 1.5× instead, to waste less).

Amortized is also not average-case: it makes no probabilistic claim, only a
guarantee that *n* appends cost O(*n*). That matters when a single slow
operation is unacceptable, which is why real-time systems often avoid
doubling arrays.

## Check your understanding

1. **Is a single append to a dynamic array O(1)?**
   - Yes, always
   - No — an append that triggers a resize copies everything, costing O(*n*)
   - No, every append is O(*n*)
   - Only when the array is empty

   <details>
   <summary>Answer</summary>

   No: the append that finds the array full is O(*n*), and amortized analysis
   never claims otherwise — only that *n* appends cost O(*n*) in total. Most
   appends are one write, so "every append is O(*n*)" is equally wrong.
   </details>

2. **What breaks if the array grows by a fixed 10 slots instead of doubling?**
   - Nothing — it is the same cost
   - Resizes stay just as expensive but stop getting rarer, so *n* appends
     become about *n*² work
   - It uses more memory
   - Appends become O(log *n*)

   <details>
   <summary>Answer</summary>

   The resizes stop thinning out. Doubling works because each buys twice the
   room the last did, so they get rarer as fast as they get costlier; a fixed
   increment gives *n*/10 resizes each copying O(*n*), which is quadratic. It
   actually wastes *less* memory — that is the trade against it.
   </details>

3. **How does amortized differ from average-case analysis?**
   - They are two names for the same thing
   - Amortized is a worst-case guarantee about a sequence of operations;
     average-case is a claim about likely inputs
   - Amortized only applies to arrays
   - Average-case is always the more pessimistic of the two

   <details>
   <summary>Answer</summary>

   Amortized makes no probabilistic assumption: *any* sequence of *n* appends
   costs O(*n*), guaranteed. Average-case reasons about likely inputs, so an
   unlucky one can beat it. Amortized reasoning applies broadly — hash table
   resizing, union-find — and neither is uniformly more pessimistic.
   </details>

## Practice

Try [Design HashMap on LeetCode](https://leetcode.com/problems/design-hashmap/)
— picking a capacity and a moment to grow makes the trade real.
