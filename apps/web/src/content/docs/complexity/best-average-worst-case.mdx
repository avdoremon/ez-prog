---
title: Best, Average, and Worst Case
description: The same algorithm on the same array can be cheap or ruinous — which case you quote decides what you are actually promising.
order: 237
difficulty: beginner
estimatedMinutes: 7
viz: linear-search
prerequisites: [/complexity/big-o, /algorithms/quick-sort]
languages: [js]
---

import Viz from '../../../components/Viz.astro';

"How long does linear search take on ten values?" has no single answer — it
depends on where the value sits.

<Viz id="linear-search" />

This run looks for 23 and finds it at index 5: six checks. Open **Try your
own input** and change only `target`. Set `2` and the run ends after one
check; set `100`, which is absent, and it costs all ten.

## Three questions, not one

- **Best case** — the cheapest input of that size: the target is first. One
  check, O(1).
- **Worst case** — the most expensive: absent, or last. *n* checks, O(*n*),
  and a guarantee — nothing can be slower.
- **Average case** — the mean over the inputs you expect. At a uniformly
  random position, about *n*/2 checks: still O(*n*).

Only the average assumes something — a distribution real data may not match.
Best and worst are the ends of the range, so they assume nothing.

## When the cases sit in different classes

<Viz id="quick-sort" />

Quick sort partitions around the last value of each range. The eight
shuffled values above cost 16 comparisons. Paste
`{ "arr": [1, 2, 3, 4, 5, 6, 7, 8] }` into the editor — already sorted — and
the count climbs to 28.

Every pivot is now the largest value left, so each partition peels off one
element instead of halving the range: *n* − 1 comparisons, then *n* − 2,
down to 1 — *n*(*n* − 1)/2 in all. Shuffled input averages O(*n* log *n*);
sorted input is O(*n*²).

Linear search's cases differ by a constant factor, so any of them tells much
the same story. Quick sort's differ by a whole growth class, so "quick sort
is O(*n* log *n*)" is an average-case claim that omits the case real data
hits most easily: input already in order. O(·) bounds growth without saying
*which* case, and a claim naming none is unfinished.

## Try it

```js
function checksToFind(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) return i + 1;
  }
  return arr.length;
}

const arr = [2, 5, 8, 12, 16, 23, 38, 56, 72, 91];
const each = arr.map((v) => checksToFind(arr, v));
const total = each.reduce((a, b) => a + b, 0);

console.log('best   ', Math.min(...each));      // 1
console.log('worst  ', checksToFind(arr, 100)); // 10
console.log('average', total / each.length);    // 5.5
```

## Trade-offs

Design against the worst case when slow means broken: a request deadline, a
control loop, a queue that backs up until it falls over. Design against the
average when work happens in bulk and only the total matters. Never design
around the best case — the cheapest input is the one you can least rely on
being handed.

When a bad worst case is easy to hit, change the algorithm, not the wording:
a random or median-of-three pivot leaves quick sort's O(*n*²) intact but
stops sorted input from triggering it.

## Check your understanding

1. **Why is sorted input the worst case for the quick sort above?**
   - Sorted arrays take longer to read
   - The last-element pivot is always the largest, so each partition removes
     one value instead of splitting the range in two
   - Comparisons cost more between ordered values
   - It is not — sorted input is its best case

   <details>
   <summary>Answer</summary>

   The last element is the largest on sorted input, so the partition leaves
   *n* − 1 values on one side and none on the other — the opposite of the
   even split quick sort needs, and why this is its worst case, not its
   best. Reading and comparing cost the same whatever the order; the
   shape of the split does all the damage.
   </details>

2. **A service must answer every request inside a fixed deadline. Which case
   should you design against?**
   - Best, so the common path is as fast as possible
   - Average, since most requests are typical
   - Worst, since the deadline applies to every request individually
   - None — deadlines are an operations problem, not an algorithms one

   <details>
   <summary>Answer</summary>

   The worst case: a deadline every request must meet is a promise about the
   slowest one. An average inside it is consistent with a tail that blows
   straight through, and the best case describes only the luckiest request.
   Nor is this only an operations problem — which algorithm you pick is
   what decides how bad that tail can get.
   </details>

## Practice

Try [Kth Largest Element in an Array on LeetCode](https://leetcode.com/problems/kth-largest-element-in-an-array/) —
quickselect averages O(*n*) and decays to O(*n*²) on that very pivot choice.
